        float depth = vec4(texture2D(depthtex0, texcoord).r).r

vec4 saturate(vec4 inputColor, float saturation) {
    vec4 ReturnColor;
    if (inputColor.r == inputColor.g && inputColor.r == inputColor.b) {

    } else if (inputColor.r > inputColor.g && inputColor.r > inputColor.b) {
        
    }
}